[package]
name = "worldcraft-dd-poc"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
timely = "0.25"
differential-dataflow = "0.18"
